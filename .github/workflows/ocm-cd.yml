name: OrangeCashMachine CD
# 🚧 Deshabilitado — deploy manual via SSH hasta que se defina deploy_ocm.sh
# El CD anterior era para un proyecto distinto (freqtrade BOTS).
# Re-habilitar cuando: deploy_ocm.sh esté implementado.
on:
  workflow_dispatch:  # solo manual, nunca automático

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CD pendiente de implementación — ver deploy.py"
